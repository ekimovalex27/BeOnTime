<Page
    x:Class="BeOnTime.FavoritesEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BeOnTime"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

  <Grid>
    <StackPanel Orientation="Vertical" Margin="0,20,0,0">
      <StackPanel Orientation="Horizontal">
        <TextBlock x:Uid="FavoritesItemName" Text="Название" Width="100" Foreground="White"/>
        <TextBox x:Name="txtName" Text="" Width="250" Foreground="White" BorderThickness="0,0,0,2"/>
      </StackPanel>

      <StackPanel Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center">
        <Button x:Name="cmdOk" x:Uid="cmdOk" Content="Сохранить" Width="150" Click="cmdOk_Click" Foreground="White" BorderBrush="Transparent" Background="Transparent" BorderThickness="0,0,0,2"/>
        <Button x:Name="cmdCancel" x:Uid="cmdCancel" Content="Отменить" Width="150" Margin="40,0,1,0" Click="cmdCancel_Click" Foreground="White" BorderBrush="Transparent" Background="Transparent" BorderThickness="0,0,0,2"/>
      </StackPanel>
    </StackPanel>
    
  </Grid>
</Page>
